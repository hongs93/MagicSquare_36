# 4×4 Magic Square — AC-FR-01-01 GREEN Wave 0 완료 보고서 (C1~C6)

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_XX |
| 문서 유형 | GREEN Wave 0 완료·ECB 30 passed |
| TDD phase | **GREEN** (최소 구현; REFACTOR 제외) |
| 작성 기준일 | 2026-05-29 |
| 브랜치 | `stabilize/green` |
| 범위 | AC-FR-01-01 **C1~C6** + AC-FR-01-05 Control 격리 |
| SSOT | `Report/10`, `Report/11`, `docs/PRD_MagicSquare.md` §8.1, `tests/constants.py` |

---

## 1. 목적

ECB Track A GREEN Wave 0의 **잔여 커밋 묶음(C1~C6)**을 완료하여 Report/06 RED 30건 전부를 통과시킨다.

| 목표 | 결과 |
|------|------|
| C1 `grid=None` | ✅ 13건 (C0·C1 선행 + 회귀) |
| C2 `grid=[]` | ✅ 3건 (Report/11 선행) |
| C3 `grid=[[]]*4` | ✅ 2건 |
| C4 `grid_3x4` | ✅ 6건 |
| C5 fixture 루프 | ✅ 1건 |
| C6 Control 격리 | ✅ 5건 |
| **회귀** | ✅ **30 passed** |

---

## 2. 구현 변경

### 2.1 `src/magicsquare/entity/constants.py` (신규)

```python
GRID_SIZE: int = 4
```

리터럴 `4` 금지 규칙 준수 — 행·열 차원 검증 SSOT.

### 2.2 `src/magicsquare/boundary/validator.py`

| 분기 | 조건 | 응답 |
|------|------|------|
| C1 | `grid is None` | `INVALID_SIZE` |
| C2 | `not grid` (행 0) | `INVALID_SIZE` |
| C3·C4 | `len(grid) != GRID_SIZE` 또는 `len(row) != GRID_SIZE` | `INVALID_SIZE` |
| FR-02+ | 4×4 정상 크기 | `NotImplementedError` |

### 2.3 `src/magicsquare/control/solver.py` (신규)

| 메서드 | 동작 |
|--------|------|
| `handle(grid)` | `BoundaryValidator.validate()` 호출; size-invalid 시 `FailureResult` 반환, `resolve()` **미호출** |
| `resolve(grid)` | FR-02~05 미구현 — `NotImplementedError` |

AC-FR-01-05: size-invalid 입력에서 Domain resolver(`resolve`) 0회 호출 검증 통과.

---

## 3. pytest 실행 결과

### 3.1 커밋 묶음별 필터

| 커밋 | 명령 | 결과 |
|------|------|------|
| C1 | `-k none_grid` | ✅ 8 passed (이름에 `none_grid` 없는 C1 테스트 5건 포함해 파일 전체 13건) |
| C2 | `-k empty_list` | ✅ 3 passed |
| C3 | `-k four_empty_rows` | ✅ 2 passed |
| C4 | `-k 3x4` | ✅ 6 passed |
| C5 | `::test_size_only_fixtures_never_use_valid_4x4_contract` | ✅ 1 passed |
| C6 | `tests/unit/control/` | ✅ 5 passed |

### 3.2 ECB 전체 회귀

```powershell
python -m pytest tests/unit/boundary/ tests/unit/control/ -v
```

**결과:** ✅ **30 passed**

### 3.3 커버리지 (DEF-004 부분 측정)

```text
src/magicsquare/boundary/responses.py   100%
src/magicsquare/boundary/validator.py    90%  (line 40: valid 4×4 NotImplementedError)
src/magicsquare/control/solver.py        77%  (resolve 경로 미실행)
TOTAL boundary+control                    88%
```

DEF-004: 커버리지 표 출력 가능 — Boundary ≥85% 목표 **달성** (boundary 패키지 기준).

---

## 4. 결함 목록 갱신

| ID | 이전 | 본 Wave 후 |
|----|------|------------|
| DEF-001 | Open | **Closed** |
| DEF-002 | Open | **Closed** |
| DEF-003 | Open | **Closed** |
| DEF-004 | Open | **Partial Close** — 측정 가능; 전체 TOTAL 90%는 후속 Wave |

---

## 5. GREEN 커밋 묶음 최종 (C0~C6)

| 커밋 | BV | 상태 |
|------|-----|------|
| **C0** | `FailureResult` | ✅ |
| **C1** | `None` | ✅ |
| **C2** | `[]` | ✅ |
| **C3** | `[[]]*4` | ✅ |
| **C4** | `3×4` | ✅ |
| **C5** | fixture 루프 | ✅ |
| **C6** | Solver 격리 | ✅ |

---

## 6. 범위 제외 (본 Wave)

- REFACTOR (`_invalid_size()` 추출, Solver DI 등)
- FR-02~05 (빈칸·범위·중복·resolve 도메인)
- Dual-Track D1~D6 (`src/boundary/`, `tests/boundary/`)
- `tests/` 수정

---

## 7. 다음 단계

1. **Dual-Track D1** — `InputValidator` AC-FR-01-01 GREEN
2. **FR-02** — 빈칸 2개 검증 (AC-FR-01-02)
3. **REFACTOR Wave** — Solver DI, validator 헬퍼 추출 (별도 커밋)
4. **DEF-004** — 전체 `src` TOTAL 90% 재측정

---

## 8. 변경 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| 1.0 | 2026-05-29 | GREEN Wave 0 C1~C6 완료·30 passed·DEF-001~003 Close |

---

*본 문서는 ECB AC-FR-01-01 GREEN Wave 0 완료 시점의 스냅샷이다.*
