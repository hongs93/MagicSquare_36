# 4×4 Magic Square — AC-FR-01-01 RED 테스트·결함 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_XX |
| 문서 유형 | 테스트 계획·RED 테스트·결함 관리 (Wave 0 / Track A·B) |
| 작성 기준일 | 2026-05-29 |
| 범위 | AC-FR-01-01, AC-FR-01-05, FR-01 size 검증, PRD §8.1 INVALID_SIZE |
| 제외 | GREEN 구현, AC-FR-01-02~04, FR-02~05 기능 개발 |

---

## 1. 목적

PRD `FR-01` Acceptance Criteria 중 **AC-FR-01-01(4×4 크기 검증)** 을 대상으로, 테스트 계획 수립 → RED 테스트 작성 → 결함 문서화까지의 산출물과 실행 결과를 고정한다.

핵심 목표:

- 선행 게이트(size)에 대한 **검증 가능한 테스트 오라클** 확보
- Boundary / Control 계층 **책임 분리**를 테스트로 고정 (Domain `resolve()` 미호출)
- RED 상태(수집 실패)와 GREEN 진입 조건을 명시

---

## 2. 선행 조건 및 샘플 예제 선정

### 2.1 선택 AC

| 항목 | 값 |
|------|-----|
| AC ID | **AC-FR-01-01** (부가: **AC-FR-01-05**) |
| FR | FR-01 Input Verification |
| BR | BR-01 (4행 4열) |
| Test Case | TS-E-01 |
| 컴포넌트 | `BoundaryValidator`, `Solver` |

### 2.2 대표 입력·기대 (워크숍 계약)

| 필드 | 값 |
|------|-----|
| 입력 | `grid = None` |
| code | `INVALID_SIZE` |
| message | `Grid must be 4x4.` |
| Domain | `resolve()` **0회** 호출 |

### 2.3 후보군 대비 선정 근거

| 후보 그룹 | AC | 선정 여부 |
|-----------|-----|-----------|
| 크기 검증 | AC-FR-01-01 | ✅ Wave 0 최상단 |
| 빈칸 개수 | AC-FR-01-02 | ❌ 선행 조건 아님 |
| 값 범위 | AC-FR-01-03 | ❌ |
| 중복 | AC-FR-01-04 | ❌ |
| `grid=None` (A-1) | — | ✅ Report 02 §8 Wave 0 null 경계 |

---

## 3. 산출물 목록

| 산출물 | 경로 | 설명 |
|--------|------|------|
| 테스트 계획서 | `docs/test_plan.md` | 범위·경계값·mock 전략·pytest-cov |
| RED 테스트 (Boundary) | `tests/unit/boundary/test_boundary_validator_size.py` | 25 tests |
| RED 테스트 (Control) | `tests/unit/control/test_solver_size_validation_gate.py` | 5 tests |
| 테스트 오라클 상수 | `tests/constants.py` | `INVALID_SIZE` / message 고정 |
| Fixture | `tests/conftest.py` | `grid_3x4`, `grid_four_rows_zero_cols` |
| 결함 목록 | `defect_list.md` | DEF-001~004 |
| README 체크리스트 | `README.md` § RED 단계 To-Do | GFM `- [ ]` 체크리스트 |
| 대화 기록 | `Prompting/06. cursor_magicsquare_ac-fr01-01-red-test-Prompt.md` | Transcript export |

---

## 4. 테스트 설계 요약

### 4.1 아이템 타입별 테스트 (각 5건, 총 30건)

| 클래스 | 유형 | 검증 포인트 |
|--------|------|-------------|
| `TestNormalFailureReturn` | 정상 실패 반환 | `grid=None` → code/message, 예외 없음 |
| `TestBoundaryValues` | 경계값 | `[]`, `[[]]*4`, 3×4 |
| `TestMessageIdentity` | 메시지 동일성 | PRD §8.1 문구 문자·길이·공백 |
| `TestFailureResponseType` | 구조체 | `FailureResult`, pydantic 스키마 |
| `TestAcFr0101ScopeRestriction` | 범위 제한 | AC-FR-01-02~04 코드 미반환 |
| `TestDomainIsolation` | 격리 | `resolve()` mock 0회 (AC-FR-01-05) |

### 4.2 명명·구조 규칙

- pytest + AAA (Given-When-Then 주석)
- 함수명: `test_[입력조건]_[기대동작]_[검증포인트]`
- docstring: `AC-FR-01-01, PRD §8.1 INVALID_SIZE` 한 줄
- 인라인: `# AC-FR-01-01`

### 4.3 범위 제외 (명시)

- 4×4 정상 계약 충족 격자 (FR-02~05)
- `INVALID_BLANK_COUNT`, `INVALID_RANGE`, `DUPLICATE_NONZERO` 시나리오

---

## 5. pytest 실행 결과 (RED)

### 5.1 환경

- Python 3.13.13, pytest 9.0.3, pytest-cov 7.1.0
- 가상환경: `.venv`
- 명령: `python -m pytest tests/unit/boundary/ tests/unit/control/ -v`

### 5.2 결과

| 항목 | 값 |
|------|-----|
| collected | **0 items** |
| errors | **2** (수집 단계) |
| passed | 0 |

### 5.3 실패 유형

```text
ModuleNotFoundError: No module named 'magicsquare.boundary.responses'
ModuleNotFoundError: No module named 'magicsquare.control.solver'
```

**해석:** RED 의도에 부합. 테스트는 존재하나 **대상 모듈 미구현**으로 assertion 단계까지 도달하지 못함.

### 5.4 커버리지 (`--cov=src`)

수집 중단 시 pytest-cov **리포트 표 미출력** (DEF-004). GREEN 후 재측정 필요.

---

## 6. 결함 요약 (`defect_list.md`)

| ID | Severity | 요약 | 상태 |
|----|----------|------|------|
| DEF-001 | Critical | `boundary/responses.py` 없음 (`FailureResult`) | Open |
| DEF-002 | Critical | `boundary/validator.py` 없음 (`BoundaryValidator`) | Open |
| DEF-003 | Critical | `control/solver.py` 없음 (`Solver`) | Open |
| DEF-004 | Medium | 수집 실패로 coverage 표 미출력 | Open |

---

## 7. GREEN 진입 조건 (Exit Criteria)

1. `src/magicsquare/boundary/responses.py` — `FailureResult`
2. `src/magicsquare/boundary/validator.py` — size 검증, `INVALID_SIZE` 반환
3. `src/magicsquare/control/solver.py` — `handle()` + size-invalid 시 `resolve()` 미호출
4. `python -m pytest tests/unit/boundary/ tests/unit/control/` → **30 passed**
5. `--cov=src --cov-report=term-missing` → Boundary ≥85% (목표)
6. `defect_list.md` DEF-001~003 Closed, README 결함 체크리스트 2번 항목 완료

---

## 8. Traceability

| Business Rule | FR | AC | Test | Component | Artifact |
|---------------|-----|-----|------|-----------|----------|
| BR-01 | FR-01 | AC-FR-01-01 | TS-E-01, 30 RED tests | BoundaryValidator | `test_boundary_validator_size.py` |
| — | FR-01 | AC-FR-01-05 | 5 isolation tests | Solver | `test_solver_size_validation_gate.py` |

---

## 9. 다음 단계

1. GREEN: DEF-001 → DEF-002 → DEF-003 순 최소 구현
2. REFACTOR: 의존성 주입·상수 `entity/constants` 분리 (magic number 금지)
3. AC-FR-01-02~04용 별도 TP 문서 및 RED 테스트 파일 분리
4. `pyproject.toml` dev에 `pytest-cov` 공식 추가 검토

---

## 10. 변경 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| 1.0 | 2026-05-29 | AC-FR-01-01 테스트 계획·RED·결함·실행 결과 통합 보고 |

---

*본 문서는 구현 코드 본문을 포함하지 않으며, RED 단계 실행 스냅샷을 기록한다.*
