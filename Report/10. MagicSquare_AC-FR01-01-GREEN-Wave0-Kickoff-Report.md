# 4×4 Magic Square — AC-FR-01-01 GREEN Wave 0 착수 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_XX |
| 문서 유형 | GREEN Wave 0 착수·최소 구현·커밋 묶음 계획 |
| TDD phase | **GREEN** (최소 구현; REFACTOR 제외) |
| 작성 기준일 | 2026-05-29 |
| 브랜치 | `stabilize/green` |
| 범위 | AC-FR-01-01, AC-FR-01-05 (Wave 0 size 게이트) |
| SSOT | `Report/06`, `docs/test_plan.md`, `tests/constants.py`, `README.md` § GREEN |

---

## 1. 목적

Report/06 ECB Full RED(30건)에 대해 **GREEN Wave 0**을 착수한다. 본 문서는 다음을 고정한다.

| 목표 | 설명 |
|------|------|
| 수집 해소 | `ModuleNotFoundError` → pytest 수집·단일 테스트 실행 가능 |
| 최소 GREEN | `grid=None` → `INVALID_SIZE` / `Grid must be 4x4.` |
| 커밋 전략 | RED 30건을 C0~C6으로 분할 커밋 (README 체크리스트) |
| 이중 트랙 | ECB(`magicsquare.*`)와 Dual-Track(`boundary.*`) 구현 경로 분리 명시 |

---

## 2. 선행 RED 상태 (변경 없음)

| 산출물 | 경로 | 건수 | 상태 |
|--------|------|------|------|
| ECB Boundary RED | `tests/unit/boundary/test_boundary_validator_size.py` | 25 | ✅ assert 유지 |
| ECB Control RED | `tests/unit/control/test_solver_size_validation_gate.py` | 5 | ✅ assert 유지 |
| Dual-Track Skeleton | `tests/boundary/`, `tests/entity/` | 23 | ✅ `pytest.fail` 유지 |
| 오라클 | `tests/constants.py` | — | `INVALID_SIZE`, `Grid must be 4x4.` |

---

## 3. 본 Wave 산출물 (구현·문서)

### 3.1 ECB Track — `src/magicsquare/boundary/`

| 파일 | 내용 |
|------|------|
| `responses.py` | `FailureResult` dataclass (`code`, `message`, `is_failure=True`); 상수 `INVALID_SIZE_*` |
| `validator.py` | `BoundaryValidator.validate()` — `grid is None`만 `FailureResult` 반환; 그 외 `NotImplementedError` |

### 3.2 Dual-Track Track — `src/boundary/`

| 파일 | 내용 |
|------|------|
| `schemas.py` | Pydantic `FailureResponse` (`type`, `code`, `message`); `ERROR` / `INVALID_SIZE` 상수 |
| `input_validator.py` | `InputValidator.validate()` — `grid is None`만 `FailureResponse` 반환 |
| `__init__.py` | public export |

> **주의:** ECB 테스트는 `magicsquare.boundary.*`만 import. Dual-Track `src/boundary/`는 별도 Wave(D1~) 대상.

### 3.3 문서

| 산출물 | 경로 |
|--------|------|
| GREEN 커밋 묶음·체크리스트 | `README.md` § GREEN 단계 To-Do |
| 대화 기록 | `Prompting/09. cursor_magicsquare_ac-fr01-01-green-wave0-Prompt.md` |

---

## 4. pytest 실행 결과

### 4.1 환경

- Python 3.13.x, pytest 8.x / 9.x, `.venv` (Windows)
- 브랜치: `stabilize/green`

### 4.2 ECB — C0·C1 부분 GREEN

| 명령 | 결과 |
|------|------|
| `pytest tests/unit/boundary/test_boundary_validator_size.py --collect-only` | ✅ 수집 가능 (DEF-001 해소) |
| `pytest …::test_none_grid_returns_failure_invalid_size_code` | ✅ **1 passed** |
| `pytest tests/unit/boundary/test_boundary_validator_size.py -k none_grid` | 🔄 C1 잔여 12건 (동일 `None` 분기로 통과 예상) |
| `pytest tests/unit/boundary/ tests/unit/control/ -v` | ❌ 29건 실패/미구현 (`NotImplementedError`, DEF-003) |

### 4.3 Dual-Track — 스모크

```text
python -c "from boundary.input_validator import InputValidator; ..."
→ ERROR INVALID_SIZE Grid must be 4x4.
```

`tests/boundary/test_ac_fr_01_01_input_validation.py`는 **미존재** — D1 RED(assert Full) 선행 필요.

---

## 5. 결함 목록 갱신 (DEF)

| ID | 이전 | 본 Wave 후 |
|----|------|------------|
| DEF-001 | Open | **Partial Close** — `FailureResult` 구현·수집 통과 |
| DEF-002 | Open | **Partial Close** — `None` 분기만; `[]`/3×4 등 미구현 |
| DEF-003 | Open | Open — `magicsquare.control.solver` 미구현 |
| DEF-004 | Open | Open — 30건 미통과로 커버리지 미측정 |

---

## 6. GREEN 커밋 묶음 (C0~C6) — README 동기화

| 커밋 | BV | 구현 | 테스트 수 | 상태 |
|------|-----|------|-----------|------|
| **C0** | — | `FailureResult` | 수집 | ✅ |
| **C1** | BV-01 `None` | `grid is None` | 13 | 🔄 1/13 확인 |
| **C2** | BV-02 `[]` | 행 0 | 3 | ❌ |
| **C3** | BV-03 `[[]]*4` | 4×0 | 2 | ❌ |
| **C4** | BV-04 `3×4` | 행·열 ≠4 | 6 | ❌ |
| **C5** | 통합 | fixture 루프 | 1 | ❌ |
| **C6** | 격리 | `Solver` + mock | 5 | ❌ |

**30건 오름차순·pytest `-k` 필터:** `README.md` § ECB GREEN 커밋 묶음 참조.

---

## 7. Import / 패키지 정리

| 트랙 | 테스트 | 구현 | import |
|------|--------|------|--------|
| ECB | `tests/unit/boundary/` | `src/magicsquare/boundary/` | `magicsquare.boundary.*` |
| Dual-Track | `tests/boundary/` | `src/boundary/` | `boundary.input_validator` |

`pyproject.toml`: `pythonpath = ["src", "."]` — `tests/boundary/` 패키지명과 `src/boundary/` 충돌 가능; Dual-Track pytest는 ECB와 **분리 실행** 권장.

---

## 8. 범위 제외 (본 Wave)

- REFACTOR, 메시지·코드 SSOT 변경
- AC-FR-01-02~04 (빈칸·범위·중복)
- `magicsquare.control.solver` (C6 전까지)
- 4×3, 5×5, jagged (현재 RED 30건에 없음)
- `tests/` 수정 (assert 약화·삭제 금지)

---

## 9. 다음 단계

1. **C1 완료** — `-k none_grid` 13건 전부 passed 후 커밋
2. **C2~C5** — `BoundaryValidator` size 분기 순차 GREEN
3. **C6** — `Solver.handle` + `resolve()` mock 0회
4. **Dual-Track D1** — `test_ac_fr_01_01_*` RED(assert) 추가 후 `InputValidator` GREEN
5. **DEF-004** — 30 passed 후 `pytest-cov` 재측정

---

## 10. 변경 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| 1.0 | 2026-05-29 | AC-FR-01-01 GREEN Wave 0 착수·C0·C1 부분·README 커밋 묶음 초판 |

---

*본 문서는 Wave 0 GREEN 착수 시점의 스냅샷이다. REFACTOR·전체 30 passed는 후속 Wave에서 갱신한다.*
